import UserLogin from "../components/UserLogin/UserLogin";
import LoginContainer from "../components/styled/LoginContainer/LoginContainer";

function Login() {
    return (
        <LoginContainer>
            <UserLogin />
        </LoginContainer>
    );
}

export default Login;
